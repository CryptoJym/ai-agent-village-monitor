############################################
# GitHub OAuth / Auth configuration
############################################
# Required for GitHub OAuth login
GITHUB_OAUTH_CLIENT_ID="your_github_oauth_client_id"
GITHUB_OAUTH_CLIENT_SECRET="your_github_oauth_secret"

# JWT secret for signing access/refresh tokens
JWT_SECRET="change_me_to_a_long_random_secret"

# Public URLs used for redirects and CORS
PUBLIC_SERVER_URL="http://localhost:3000"   # Base URL for this server
PUBLIC_APP_URL="http://localhost:5173"      # Frontend URL to redirect to after login

# Optional explicit OAuth callback (defaults to ${PUBLIC_SERVER_URL}/auth/github/callback)
OAUTH_REDIRECT_URI=""

# Space-delimited scopes; add 'repo' only if private repo access is required
OAUTH_SCOPES="read:user read:org workflow"

# Optional cookie domain for production (e.g., ".example.com")
COOKIE_DOMAIN=""

# Optional KMS key identifier for future secrets-at-rest encryption
KMS_KEY_ID=""

############################################
# Database
############################################
# PostgreSQL connection string (required for migrations and runtime DB access)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/agent_village?schema=public"

############################################
# Misc
############################################
# Optional GitHub tokens for API integrations (non-OAuth, PATs)
GITHUB_TOKENS="ghp_xxx,ghp_yyy"

# Node environment
NODE_ENV=development

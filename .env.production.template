# Production environment template (fill with real values in your platform)

# Public URLs
PUBLIC_APP_URL=https://app.example.com
PUBLIC_SERVER_URL=https://api.example.com

# Allowed frontend origins for HTTP/WS (comma-separated)
WS_ALLOWED_ORIGINS=https://app.example.com,https://your-project.vercel.app

# Database & Redis (Railway managed)
DATABASE_URL=postgresql://user:pass@host:5432/dbname?sslmode=require
REDIS_URL=rediss://:password@host:6379/0

# Auth and security
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=
JWT_SECRET=
WEBHOOK_SECRET=

# Optional
COOKIE_DOMAIN=.example.com
OAUTH_REDIRECT_URI=https://api.example.com/auth/github/callback
GITHUB_TOKENS=

# Frontend build-time hints (Vercel)
VITE_WS_URL=wss://api.example.com

